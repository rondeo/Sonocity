type UserSettings {
    _id: String!
    userId: String!
    security_lvl: String!
}

type Mutation {
    insertUserSettings(security_lvl: String!, userId: String!): UserSettings
    updateUserConfidentialitySettings(security_lvl: String!): UserSettings
}

extend type Query {
    userSettings: UserSettings
}