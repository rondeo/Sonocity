type Chatroom {
    _id: String!
    userId0: String!
    userId1: String!
}

extend type Mutation {
    createChatroom(userId0: String!, userId1: String!): Boolean
}

extend type Query {
    chatRooms: [Chatroom]
}