type Chatroom {
    _id: String!
    userId0: String!
    userId1: String!
}

type Mutation {
    createChatroom(userId0: String!, userId1: String!): Boolean
}

extend type Query {
    chatroomId(userId0: String!, userId1: String!): Chatroom
}